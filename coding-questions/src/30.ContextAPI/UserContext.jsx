import React, { createContext } from "react";

export const UserContextInfo = createContext();

export const UserContextInfoProvider = UserContextInfo.Provider;

export const UserContextInfoConsumer = UserContextInfo.Consumer;
